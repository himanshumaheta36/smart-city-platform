server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        corsConfigurations:
          '[/**]':
            allowedOrigins: 
              - "http://localhost:3000"
            allowedMethods: "*"
            allowedHeaders: "*"
            allowCredentials: true
      routes:
        # Mobility Service - CORRIGÉ
        - id: mobility-service
          uri: http://localhost:8081
          predicates:
            - Path=/api/mobility/**
          filters:
            - RewritePath=/api/mobility/(?<segment>.*), /mobility/api/${segment}

        # Air Quality Service - CORRIGÉ
        - id: air-quality-service-soap
          uri: http://air-quality-service:8082
          predicates:
            - Path=/api/air-quality/**
          filters:
            - RewritePath=/api/air-quality/(?<segment>.*), /airquality/ws/${segment}

        # Emergency Service - CORRIGÉ
        - id: emergency-service
          uri: http://localhost:8083
          predicates:
            - Path=/api/emergency/**
          filters:
            - RewritePath=/api/emergency/(?<segment>.*), /api/${segment}

        # Events Service - CORRIGÉ
        - id: urban-events-service
          uri: http://localhost:8084
          predicates:
            - Path=/api/events/**
          filters:
            - RewritePath=/api/events/(?<segment>.*), /${segment}

        # Orchestration Service - CORRIGÉ
        - id: orchestration-service
          uri: http://localhost:8085
          predicates:
            - Path=/api/orchestration/**
          filters:
            - RewritePath=/api/orchestration/(?<segment>.*), /orchestration/${segment}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    reactor.netty: DEBUG